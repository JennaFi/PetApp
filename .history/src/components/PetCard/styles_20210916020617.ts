import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  
  root: {
    flexDirection: 'row',
    marginVertical: 5,
    borderWidth: 1,
    borderColor: '#d1d1d1',
    
    backgroundColor: '#fff',
  },
  row: {

  },
  image: {
    height: 700,
    borderRadius: 10,
    
  }
})
export default styles